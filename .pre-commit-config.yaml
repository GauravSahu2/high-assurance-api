repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.2
    hooks:
      - id: gitleaks
        # This ensures it uses your custom .gitleaks.toml paths
        args: ["detect", "--source=.", "--staged", "--verbose", "--config=.gitleaks.toml"]
