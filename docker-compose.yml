version: '3.8'
services:
  api:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "pip install -r requirements.txt && python src/main.py"
    ports:
      - "8000:8000"
    env_file:
      - .env
